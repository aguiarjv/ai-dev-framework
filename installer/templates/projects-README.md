# Managed Projects

Each folder in this directory is a managed project workspace. Add projects with
the installed `project-setup` skill so their metadata and Git worktrees use the
standard AI Dev Framework layout.
